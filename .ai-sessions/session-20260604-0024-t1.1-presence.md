# Session — V34 T1.1 + proto3-strict: presence, packed, closed enums, wire validation

- **Branch**: v1 — V34-PLAN.md Phase 1.

## Done (two parallel streams, orchestrator-verified)
### Codec proto2 presence/packed/closed enums (T1.1)
- Presence-driven default-omit: explicit-presence fields (proto2/editions/optional/
  oneof) emit when set even at zero, and an absent explicit field stays absent on
  decode (no proto3 default-fill). Cleared the dominant 269 `added: optional_*` family.
- Closed-enum unknown value routed to unknown-field set (proto2/editions-proto2),
  open enums keep it in-field.
- Feature-driven packed encode (proto2 EXPANDED, proto3/edition2023 PACKED),
  lenient decode unchanged. t/codec/proto2_presence.t.

### proto3 v34-strict edge cases (38 -> 0)
- Wire-type validation: reject wire types 6/7 + bad/overlong tags + field# overflow
  (groups 3/4 stay valid). t/codec/wire_type_validation.t.
- Timestamp/Duration nanos bounds + sign on JSON output -> JSON::WKT. t/wkt/nanos_bounds.t.
- Quoted exponential int strings ("1e5"->100000, exact-integer only). t/json/quoted_exponential_int.t.
- Any: empty {} round-trips, Any-of-Empty inlines. t/wkt/any_empty_and_no_type.t.

## Conformance progress (live v34)
386 -> 52 failures. Proto3: 0. Proto2/editions: 348 -> 52. Successes 2268 -> 2688.
Suite green: 1297 tests.

## Remaining 52 (all proto2/editions)
groups round-trip, required-field validation, extensions/MessageSet, plus tail edges.
