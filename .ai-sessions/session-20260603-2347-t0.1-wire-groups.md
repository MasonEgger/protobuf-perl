# Session — V34 T0.1: wire-layer group support

- **Branch**: v1
- Part of the V34-PLAN.md effort (full protobuf conformance incl. proto2/editions).

## Done (T0.1)
- Proto3::Wire::Tag::decode_tag no longer rejects wire types 3/4; they are
  returned as first-class SGROUP/EGROUP (proto2 groups + editions DELIMITED).
  Exported WIRE_GROUP_START/WIRE_GROUP_END.
- Proto3::Wire::skip_group($bytes,$field_number): skip a group body to its
  matching EGROUP, depth-tracking nested groups. skip_field rejects a bare
  group (needs the field number -> use skip_group).
- Codec unknown-field skip path is group-aware (skip_group for unknown SGROUP).
- Updated t/wire/tag.t (group round-trip), t/wire/wire.t (skip_group), and
  t/codec/decode_scalar.t 11.6 (unterminated group -> Wire error).

## State
Suite green: 1143 tests. proto3 still 100%. Next: T0.2 schema features/presence
scaffolding.
