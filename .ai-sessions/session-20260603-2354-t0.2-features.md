# Session — V34 T0.2: schema features/presence/editions scaffolding

- **Branch**: v1 — part of V34-PLAN.md (full protobuf conformance).

## Done (T0.2)
- NEW Proto3::Schema::Features: FeatureSet (field_presence, enum_type,
  repeated_field_encoding, message_encoding, utf8_validation, json_format) with
  edition defaults (proto2/proto3/2023) and a merge function.
- Schema::File gains edition + features; Field gains presence/default_value/
  message_encoding/is_extension/extendee/features + feature-driven is_packed +
  has_presence; Enum gains closed; Message gains extension_ranges/extensions/
  message_set_wire_format.
- Schema.resolve runs a feature-resolution pass (idempotent) and builds an
  extension registry (extensions_for).
- DEFAULTS preserve proto3 behavior exactly. NEW t/unit/features.t (58 assertions).

## State
Suite green: 1201 tests (1143 + 58). proto3 still 100%. No codec behavior changed.
Next: T0.3 bootstrap descriptor schema + regenerate conformance.fds from v34 protos.
