# Session — GitHub repo rename to protobuf-perl

- **Branch**: v1, PR #1.
- GitHub repo renamed proto3-perl -> protobuf-perl (Mason did the GitHub rename).
- git remote origin updated to git@github.com:MasonEgger/protobuf-perl.git (verified
  it resolves: v1 = 3dc95ee).
- Updated in-repo URL references: README install/clone/cpanfile-pin URLs and the
  -Ilib path examples; spec.md repository header + tree-diagram root.
- Left .ai-sessions/ historical notes and spec.md design-prose title as-is.
- No code touched; suite still 1357 green.
